import { useState, useEffect } from 'react';
import TransactionFailed from '@/components/TransactionFailed/Component';
const Dashboard = () => {

    return (
        <div>
           <TransactionFailed />
        </div>
    );
};

export default Dashboard;
